# tools.kakipo.com

細々した WEB ツール置き場です。ビルド不要の静的サイト（GitHub Pages）として公開しています。
各ツールはサブディレクトリに 1 つずつ配置し、`tools.kakipo.com/<ツール名>/` でアクセスできます。

> ルート（`tools.kakipo.com/`）のトップページは今のところ用意していません。

## ツール一覧

| ツール | パス | 説明 |
|--------|------|------|
| 鋳造価格 計算ツール | [`/yuai2026/`](https://tools.kakipo.com/yuai2026/) | ワックス原型の重量から、シルバー・真鍮などの鋳造価格を見積もる |

---

## 鋳造価格 計算ツール（`/yuai2026/`）

ワックス原型の重量から、**シルバー・真鍮**などの金属の鋳造価格を見積もる WEB ツールです。
ジュエリー制作の見積り用。単一 HTML ファイル（`yuai2026/index.html`）で動きます。

### 使い方

1. `tools.kakipo.com/yuai2026/` を開く（ローカルなら `yuai2026/index.html` をブラウザで開く）
2. **ワックス重量(g)** を入力
3. 金属（シルバー / 真鍮）を選択
4. 工賃・地金代・税込合計が自動計算されます

地金相場は日々変動するので、`⚙ 単価・係数の設定` から **地金単価** を更新できます。
変更した値はブラウザに保存されます（`初期値に戻す` で既定値に戻せます）。

### 計算式

以下の計算ロジックを実装しています。

```
金属重量 = ワックス重量 × 比重 × (1 + 湯口率)   ※湯口率は金属重量に加算する割合
工賃     = 金属重量 × 工賃単価(円/g)
地金代   = 金属重量 × 地金単価(円/g)
合計     = (工賃 + 地金代) × (1 + 税率)
```

#### 初期値（2025/5/25 時点）

| 金属 | 比重 | 工賃 (円/g) | 地金単価 (円/g) |
|------|------|------------|----------------|
| シルバー (SV) | 10.5 | 132 | 549 |
| 真鍮 | 8.6 | 154 | 33 |

※ 消費税 10% / 湯口・鋳造追加率 +2%（金属重量に対して）

---

## 公開のしくみ（GitHub Pages + 独自ドメイン）

- `main` への push で `.github/workflows/deploy-pages.yml` が走り、リポジトリ全体を GitHub Pages にデプロイします。
- ルートの `CNAME` ファイルで独自ドメイン `tools.kakipo.com` を指定しています。
- 新しいツールを追加するときは、リポジトリ直下に `<ツール名>/index.html` を置くだけで `tools.kakipo.com/<ツール名>/` で公開されます。

### 独自ドメインの DNS 設定（初回のみ）

DNS プロバイダで以下の CNAME レコードを追加します。

```
tools.kakipo.com.  CNAME  kakipo.github.io.
```

その後、**Settings → Pages → Custom domain** に `tools.kakipo.com` を設定し、証明書が発行されたら **Enforce HTTPS** を有効化します。

## ライセンス

MIT
